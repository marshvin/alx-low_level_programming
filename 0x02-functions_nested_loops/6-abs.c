#include "main.h"
/**
* _abs - computes the absolute value of an integer
* @x: - integer to be computed on
*
* Return: absolute value of int
*/
int _abs(int x)
{
if (x >= 0)
{
return (x);
}
return (-x); 
}
